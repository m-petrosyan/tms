import { createStore } from 'vuex'
import task from "@/store/modules/task";

export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    task
  }
})
